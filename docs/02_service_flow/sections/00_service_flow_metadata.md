# 로브 (Lovv) 서비스 흐름 명세서

> 문서 버전: v0.3
> 문서 상태: 검토 중 (Review)
> 기준 문서: 요구사항 명세서

> **[PRD 반영 v0.1 — 대화형 빌더]** 일정 생성 흐름이 자동 결과 화면에서 **사용자 주도 HITL 빌더**로 바뀐다:
> 가장 적합한 장소 1곳을 시작으로 **반경 내 후보(관광지 DB + 식당·카페 지도 API)를 제시 → 사용자 선택 → 다음 장소 반경 제시**를 반복(interrupt/resume).
> 후보 선택은 메인 대화창과 격리된 **서브 윈도우(사이드바)** 에서 진행한다. 상세: `../98_prd/interactive_builder_prd.md`.
